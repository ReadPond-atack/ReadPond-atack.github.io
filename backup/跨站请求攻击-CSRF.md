&emsp;&emsp;跨站请求攻击，简单地说，是攻击者通过一些技术手段欺骗用户的浏览器去访问一个自己曾经认证过的网站并执行一些操作（如发邮件、发消息、甚至财产操作：转账、购买商品等）。由于浏览器曾经认证过，所以被访问的网站会认为是真正的用户操作而去执行。这利用了web中用户身份认证的一个漏洞：简单的身份验证只能保证请求发自某个用户的浏览器，却不能保证请求本身是用户自愿发出的。

#### CSRF攻击原理及过程

&emsp;1.用户C打开浏览器，访问受信任网站A，输入用户名和密码请求登录网站A；
&emsp;2.在用户信息用过验证后，网站A产生Cookie信息并返回给浏览器，此时用户登录网站A成功，可以正常发送请求到网站A；
&emsp;3.用户未退出网站A之前，在同一浏览器中打开一个TAB页访问网站B；
&emsp;4.网站B接受到用户请求后，返回一些攻击性代码，并发出一个请求要求访问第三方站点A；
&emsp;5.浏览器在接收到这些攻击性代码后，根据网站B的请求，在用户不知情的情况下携带Cookie信息，向网站A发出请求。网站A并不知道该请求其实是由B发起的，所以会根据用户C的Cookie信息以C的权限处理该请求，导致来自网站B的恶意代码被执行。

#### 漏洞条件

&emsp;1. 被害用户已经完成身份认证
&emsp;2. 新请求的提交不需要重新身份认证或确认机制
&emsp;3. 攻击者必须了解Web APP请求的参数构造
&emsp;4. 引诱用户触发攻击的指令（社工）

#### 防御绕过

&emsp;1. 删除CSRFTOKEN
&emsp;2. 将POST改为GET
&emsp;3. CSRFTOKEN未和身份凭证绑定

#### 加固

- 二次密码验证
- Referer来源验证
&emsp;- Referer：获取的是上一条历史记录的url
- 添加CSRFTOKEN
&emsp;- 用户每次访问关键页面时，服务端通过cookie下发一个暗号“-csrftoken”是一个随机生成的字符串
- 限制cookie同源
- 在HTTP头中自定义属性并验证。

各种功能点：密码修改处、点赞、转账、注销、删除

### 总结

&emsp;&emsp;跨站请求攻击发生点在攻击者这里，攻击者首先构造一个带有攻击代码链接，将这个链接伪装好分享给受害者，受害者在点开这个链接之后，浏览器就会将受害者的cookie信息与攻击代码一起发送给服务器，此时服务器会认为这是来自受害者的攻击。
&emsp;&emsp;在这个过程中攻击者只知道受害者将这个链接点了还是没点，关于受害者的信息攻击者一无所知。